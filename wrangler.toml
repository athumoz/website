name = "athu-gemini-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# No KV or Durable Objects needed — rate limiter is in-memory per isolate.
# For production you could swap it for a KV-backed counter for multi-region consistency.

[vars]
# Non-secret vars go here. GEMINI_API_KEY is set via `wrangler secret put` instead.

# Optional: restrict to your zone
# route = { pattern = "athu.mz/api/*", zone_name = "athu.mz" }
